var infinity = infinity || {};

infinity.partials = infinity.partials || {};

infinity.partials = {

	timeline: {
		item: '<li class="track"><img class="track-image" src="{{image_src}}">{{track_artist_name}} - {{track_title}}</li>'
	}
}